# 2MCT-S4-IndustryProject

### Dependencies

>pip install numpy scikit-learn pandas matplotlib seaborn scipy tensorflow mediapipe opencv-python numpy requests pathlib pyyaml h5py

### Installation guide server

Install the latest python interpreter on the host machine: https://www.python.org/downloads/

Install the required python modules listed above under "dependencies". 

Clone the repository using the command: git clone https://github.com/Tuur123/2MCT-S4-IndustryProject.git

Request an authentication key for your google drive: https://developers.google.com/oauthplayground/

Start the server using the command: server.py -f (fps) -t (treshhold) -c (maxClients) -a (google drive auth key)
